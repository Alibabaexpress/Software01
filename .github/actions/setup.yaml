name: .NET Setup
description: Setup .NET SDK and restore Solution

runs:
 using: composite
 steps:
     - name: Setup .NET
       uses: actions/setup-dotnet@v5
       with: 
        dotnet-version: "10.0.x"

     - name: Restore dependencies
       shell: bash
       run: dotnet restore CSharpTesting.slnx